# Environment

This module expects the shared OMIX `r-pathway` runtime.

`postInstall.sh` installs or verifies the R packages needed by the GSVA implementation.
The shared starter image (`ghcr.io/nidap-community/omix-r-pathway:latest`) should already include all dependencies.
