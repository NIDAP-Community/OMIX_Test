# Results

Code Ocean captures files written to `/results`.

This runtime writes:

- `gsva_results.csv` — Enrichment scores matrix (gene sets × samples)
- `gsva_heatmap.png` — Heatmap of enrichment scores
