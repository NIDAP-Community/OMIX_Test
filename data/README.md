# Data

Production inputs should be mounted by the target execution platform. Do not commit production DEG tables or generated analysis data to this repository.

Expected mounted files:

- `/data/params.json`
- `/data/deg_table.csv`

The `example_inputs/` directory contains a tiny CSV and parameters file for dry-run validation only.
