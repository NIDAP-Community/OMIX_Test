# Environment

This module expects the shared OMIX `r-pathway` runtime.

`postInstall.sh` installs or verifies the R packages needed by the wrapper and L2P implementation. The first Code Ocean example should use the shared starter image when available, then run this script only to fill missing dependencies.
