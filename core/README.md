# OMIX Core

Shared scientific/runtime R utilities for OMIX modules.

Keep this package small. Add code here only when multiple independent modules need the same runtime behavior. Repo governance, CI checks, sync helpers, and module-contract validation belong under `scripts/` and `tests/`, not here.
